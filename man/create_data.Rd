% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_data.R
\name{create_data}
\alias{create_data}
\title{Simulate a matched case-control dataset}
\usage{
create_data(
  set = "SetA",
  set.x = "SetA",
  set.het = "SetHet",
  snr = "normal",
  snr.x = "normal",
  snr.het = "normal",
  p = 10,
  N = 5e+05,
  d = 1000,
  exp.effect = 0.6,
  het.exposure.strength = 0.6,
  n_strata = 500,
  n_het_effects = 2,
  k = 4,
  rr = 0,
  rho = 0,
  corr = c("Toeplitz", "iid"),
  alpha.fac = 1,
  int = -2,
  int.x = -1,
  clr_mode = FALSE
)
}
\arguments{
\item{set}{Character. Name of the base simulation scenario for outcome generation.}

\item{set.het}{Character. Name of the scenario used for heterogeneous exposure effects.}

\item{snr}{Character. Signal-to-noise ratio for main outcome effects. Options: "normal", "strong", "weak".}

\item{snr.x}{Character. Signal-to-noise ratio for exposure model. Options: "normal", "strong", "weak".}

\item{snr.het}{Character. Signal-to-noise ratio for heterogeneous exposure effects. Options: "normal", "strong", "weak".}

\item{p}{Integer. Number of covariates (confounders) to simulate.}

\item{N}{Integer. Total number of individuals in the population.}

\item{d}{Integer. Number of districts (clusters) in the population.}

\item{exp.effect}{Numeric. Baseline log-odds effect of exposure on outcome.}

\item{het.exposure.strength}{Numeric. Strength of effect modification by exposure heterogeneity.}

\item{n_strata}{Integer. Number of matched strata (cases) to sample.}

\item{n_het_effects}{Integer. Number of covariates that contribute to heterogeneous effects.}

\item{k}{Integer. Number of individuals per stratum (1 case + k-1 controls).}

\item{rr}{Numeric. Correlation coefficient for compound symmetry covariance structure.}

\item{rho}{Numeric. Correlation parameter for Toeplitz covariance structure.}

\item{corr}{Character. Covariate correlation structure. Options: "iid", "cs", "Toeplitz".}

\item{alpha.fac}{Numeric. Scaling factor for random district and sex effects.}

\item{int}{Numeric. Intercept for outcome model.}

\item{int.x}{Numeric. Intercept for exposure model.}

\item{clr_mode}{Logical. If TRUE, output coefficient names in CLR-style; otherwise use boosting-style names.}
}
\value{
A list with two elements:
\describe{
\item{data}{A data.frame containing the matched case-control dataset. Columns include:
exposure (X), strata, covariates (Z1,...,Zp), outcome (y), and sex (s).}
\item{true_coefs}{A named numeric vector of the true coefficients for main and heterogeneous effects.}
}
}
\description{
Generates a matched case-control dataset with binary exposure,
continuous and binary confounders, district strata, and optional
heterogeneous exposure effects.
}
\examples{
\dontrun{
sim <- create_data(
  set = "SetA",
  set.het = "SetHet",
  p = 10,
  n_strata = 500,
  k = 4
)
}

}
